/** Homepage hero image. Mirrors backend `HeroImageOut`. */
export interface HeroImage {
  id: string;
  city: string;
  imageUrl: string;
  source: string;
  photographerName: string;
  photographerUrl: string;
}
